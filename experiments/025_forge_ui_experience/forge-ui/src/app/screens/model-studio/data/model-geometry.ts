/**
 * Shared geometry for Model Studio.
 *
 * Entity dimensions are design geometry.
 * Layout and relationship routing both consume these values.
 */
export const MODEL_NODE_WIDTH = 230;
export const MODEL_NODE_HEIGHT = 210;
